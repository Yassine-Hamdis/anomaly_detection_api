# 🚨 Anomaly Detection API (PCA + KMeans)

A FastAPI-based microservice that detects anomalies in server usage metrics using **PCA** for dimensionality reduction and **KMeans** clustering.

## 📂 Project Structure
